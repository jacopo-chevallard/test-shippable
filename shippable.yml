language: c


integrations:                              
    hub:
      - integrationName: Docker
        type: docker                        

branches:
  only:
    - master

compiler:
  - gcc

build:

  pre_ci_boot:
    image_name: eclake/beaglebaseimage
    image_tag: 
    pull: true                              #required, false if image already built or pulled in pre_ci
    #options:
    #env:

  ci:
    - mkdir -p build
    - cd build
    - cmake -DCMAKE_Fortran_COMPILER=gfortran ..
    - make
    - ./hello_world

  post_ci:
    - echo "true"
